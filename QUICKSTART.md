# Быстрый старт RetailCRM MCP Server

Этот документ поможет вам быстро запустить MCP сервер за 5 минут.

## Вариант 1: Запуск с Docker (Рекомендуется)

### Требования
- Docker и Docker Compose установлены

### Шаги

1. **Скачать проект**
   ```bash
   git clone <repository-url>
   cd retailcrm-mcp-server
   ```

2. **Создать конфигурацию**
   ```bash
   cp .env.example .env
   ```

3. **Отредактировать .env**
   ```bash
   nano .env
   ```
   
   Замените:
   ```
   RETAILCRM_URL=https://your-instance.retailcrm.ru
   RETAILCRM_API_KEY=your-api-key-here
   ```

4. **Запустить контейнер**
   ```bash
   docker-compose up -d
   ```

5. **Проверить статус**
   ```bash
   docker-compose logs -f retailcrm-mcp
   ```

6. **Готово!** Сервер доступен по адресу: `http://localhost:3000`

---

## Вариант 2: Локальный запуск

### Требования
- Node.js 20+
- npm

### Шаги

1. **Скачать проект**
   ```bash
   git clone <repository-url>
   cd retailcrm-mcp-server
   ```

2. **Установить зависимости**
   ```bash
   npm install
   ```

3. **Создать конфигурацию**
   ```bash
   cp .env.example .env
   ```

4. **Отредактировать .env**
   ```bash
   nano .env
   ```
   
   Замените:
   ```
   RETAILCRM_URL=https://your-instance.retailcrm.ru
   RETAILCRM_API_KEY=your-api-key-here
   ```

5. **Собрать проект**
   ```bash
   npm run build
   ```

6. **Запустить сервер**
   ```bash
   npm start
   ```

7. **Готово!** Сервер доступен по адресу: `http://localhost:3000`

---

## Тестирование

### Проверить, что сервер работает

```bash
curl -X POST http://localhost:3000/query \
  -H "Content-Type: application/json" \
  -d '{
    "operation": "get_leads_count",
    "params": {
      "date_from": "2025-10-01",
      "date_to": "2025-10-31"
    }
  }'
```

Вы должны получить JSON ответ с данными о лидах.

---

## Получение API ключа RetailCRM

Если у вас еще нет API ключа:

1. Войдите в админ-панель RetailCRM
2. Перейдите в **Администратор** → **Интеграция** → **API ключи**
3. Нажмите **Создать API ключ**
4. Скопируйте ключ
5. Вставьте в .env файл

---

## Интеграция с n8n

### Быстрая интеграция

1. В n8n создайте новый workflow
2. Добавьте **HTTP Request** node
3. Настройте:
   - **Method**: POST
   - **URL**: `http://localhost:3000/query`
   - **Body**:
     ```json
     {
       "operation": "get_sales_by_source",
       "params": {
         "date_from": "2025-11-01",
         "date_to": "2025-11-30"
       }
     }
     ```

Готово! Теперь вы можете использовать MCP сервер в n8n workflows.

---

## Интеграция с Manus

### Быстрая интеграция

1. В Manus перейдите в **Settings** → **MCP Servers**
2. Нажмите **Add New Server**
3. Заполните:
   - **Name**: `RetailCRM`
   - **URL**: `http://localhost:3000`
   - **Transport**: `Streamable HTTP`
4. Нажмите **Save**

Готово! Теперь вы можете использовать MCP сервер в Manus AI агентах.

---

## Примеры запросов

### Продажи по источникам в ноябре

```bash
curl -X POST http://localhost:3000/query \
  -H "Content-Type: application/json" \
  -d '{
    "operation": "get_sales_by_source",
    "params": {
      "date_from": "2025-11-01",
      "date_to": "2025-11-30"
    }
  }'
```

### Количество лидов в октябре

```bash
curl -X POST http://localhost:3000/query \
  -H "Content-Type: application/json" \
  -d '{
    "operation": "get_leads_count",
    "params": {
      "date_from": "2025-10-01",
      "date_to": "2025-10-31"
    }
  }'
```

### Статистика по методам за 3 месяца

```bash
curl -X POST http://localhost:3000/query \
  -H "Content-Type: application/json" \
  -d '{
    "operation": "get_registration_stats",
    "params": {
      "date_from": "2025-09-01",
      "date_to": "2025-11-30"
    }
  }'
```

---

## Решение проблем

### Ошибка: "Connection refused"
- Убедитесь, что сервер запущен: `docker-compose ps` или `npm start`
- Проверьте, что порт 3000 не занят другим приложением

### Ошибка: "API key not found"
- Проверьте, что вы установили `RETAILCRM_API_KEY` в .env файле
- Убедитесь, что API ключ корректный

### Ошибка: "Cannot find module"
- Убедитесь, что вы установили зависимости: `npm install`
- Убедитесь, что вы собрали проект: `npm run build`

### Сервер не запускается
- Проверьте логи: `docker-compose logs retailcrm-mcp` или `npm run dev`
- Убедитесь, что все переменные окружения установлены

---

## Дополнительные ресурсы

- [Полная документация](README.md)
- [Примеры использования](EXAMPLES.md)
- [Интеграция с n8n](N8N_INTEGRATION.md)
- [Интеграция с Manus](MANUS_INTEGRATION.md)

---

## Следующие шаги

1. ✅ Запустить MCP сервер
2. ✅ Протестировать запросы
3. ✅ Интегрировать с n8n или Manus
4. ✅ Создать AI агентов для анализа данных

Удачи! 🚀
